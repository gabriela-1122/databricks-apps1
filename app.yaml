command: [
  "streamlit", 
  "run",
  "app.py"
]

env:
  - name: sql-warehouse
    valueFrom: "sql-warehouse"

  - name: dbr-token
    valueFrom: "dbr-token"

  - name: claude-api
    valueFrom: "claude-api"
