command: [
  "streamlit", 
  "run",
  "app.py"
]

env:
  - name: DATABRICKS_HOST
    valueFrom: "{{dbr-host}}"

  - name: HTTP_PATH
    valueFrom: "{{sql-warehouse}}"

  - name: DATABRICKS_TOKEN
    valueFrom: "{{dbr-token}"

  - name: ANTHROPIC_API_KEY
    value: "{{claude-api}}"
