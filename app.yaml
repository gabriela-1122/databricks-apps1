command: [
  "streamlit", 
  "run",
  "app.py"
]

env:
  - name: DATABRICKS_HOST
    valueFrom: "{{secrets/dq-app-secrets/DATABRICKS_HOST}}"

  - name: HTTP_PATH
    valueFrom: "{{secrets/dq-app-secrets/HTTP_PATH}}"

  - name: DATABRICKS_TOKEN
    valueFrom: "{{secrets/dq-app-secrets/DATABRICKS_TOKEN}}"

  - name: ANTHROPIC_API_KEY
    valueFrom: "{{secrets/dq-app-secrets/ANTHROPIC_API_KEY}}"
